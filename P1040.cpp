#include <stdio.h>
#include <iostream>
#include <string>
#include <string.h>
#include <algorithm>
#include <stdlib.h>
#include <map>
#include <queue>
using namespace std;
int n,a[35],ans,Max = 0,c[35],b[35],k;
bool v[35] = {false},d[35] = {false};

int dfs(int l,int r)
{
	v[x] = true;
	b[k] = x;
	k++;
	for(int i = 1;i <= n;i++)
	{
		for(int i = 1;i <= n;i++)
		{
			
		}
	}
}
int main()
{
	v[0] = true;
	cin >> n;
	v[n + 1] = true;
	for(int i = 1;i <= n;i++)
	{
		cin >> a[i];
	}
	for(int i = 1;i <= n;i++)
	{
		k = 1;
		
	}
	cout << Max << endl;
	for(int i = 1;i <= n;i++)
	{
		cout << c[i];
	}
}