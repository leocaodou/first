#include <stdlio.h>
#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	int amin,bmax,za,zb;
	cin >> za;
	cin >> amin >> bmax;
	int a,b,w,max,a0,b0;
	while(1)
	{
		if(a == -1 && b == -1)
			break;
		cin >> a >> b;
		w = (a - amin) * b;
		if(a == z)
			b = zb;
		if(w > max)
		{
			max = w;
			a0 = a;
		}
	}
	int x;
	cin >> x;
	for(int i = )
}